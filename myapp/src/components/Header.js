import React from 'react'; // imports React library from React Module
const Header = () => { // Header is the functional component
    return (       // to specify the component should render on the screen
            <div className="center">
                <h2>Contact Manager </h2>
            </div>
    );
};

export default Header;
